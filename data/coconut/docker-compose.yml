#docker-compose.yml
version: '3.7'
services:
      react:
        environment:
          - ENV_REACT=${REACT_ENV}
        image: rumblemonkey/coconut-react:${REACT_TAG}
        ports:
          - "80:80"
          - "443:443"
